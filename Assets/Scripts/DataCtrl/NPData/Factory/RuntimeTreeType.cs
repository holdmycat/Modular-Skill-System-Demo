namespace Ebonor.DataCtrl
{
    /// <summary>
    /// High-level runtime tree category (skill/behaviour/etc).
    /// </summary>
    public enum RuntimeTreeType
    {
        Unknown = 0,
        SlgSquadBehavour = 1,
    }
}
